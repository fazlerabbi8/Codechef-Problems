#include <bits/stdc++.h>
using namespace std;

void solve(int X, int Y)
{
    cout << (X - 1) + Y << "\n";
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int X, Y;
    cin >> X >> Y;

    solve(X, Y);

    return 0;
}